name: Changelog

# Fails a pull request that did not update CHANGELOG.md. Waivable with `skip-changelog`.

# `labeled` and `unlabeled` are listed because naming any type replaces the defaults.
on:
  pull_request:
    types: [opened, synchronize, reopened, labeled, unlabeled]

permissions:
  contents: read

jobs:
  changelog:
    uses: datarobot-oss/github-actions/.github/workflows/check-changelog.yaml@0.0.26
    permissions:
      contents: read
    with:
      changelog_path: CHANGELOG.md
      skip_label: skip-changelog
      skip_bot_authors: false
